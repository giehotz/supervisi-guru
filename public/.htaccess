# ----------------------------------------------------------------------
# Specify PHP Version
# ----------------------------------------------------------------------
AddHandler application/x-httpd-php84 .php

# ----------------------------------------------------------------------
# Web Fonts & Assets MIME Types
# Mencegah browser memblokir font/icon karena X-Content-Type-Options: nosniff
# ----------------------------------------------------------------------
<IfModule mod_mime.c>
    AddType font/woff                      .woff
    AddType font/woff2                     .woff2
    AddType font/ttf                       .ttf
    AddType font/otf                       .otf
    AddType application/vnd.ms-fontobject  .eot
    AddType image/svg+xml                  .svg
</IfModule>

# ----------------------------------------------------------------------
# Security and Performance Headers
# ----------------------------------------------------------------------
ServerSignature Off

# ----------------------------------------------------------------------
# Rewrite engine
# ----------------------------------------------------------------------
<IfModule mod_rewrite.c>
    Options +FollowSymlinks -Indexes
    RewriteEngine On

    # If you installed CodeIgniter in a subfolder, uncomment below
    # RewriteBase /

    # Redirect Trailing Slashes...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)/$ /$1 [L,R=301]

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Allow direct access to static asset files
    RewriteCond %{REQUEST_URI} \.(css|js|woff|woff2|ttf|eot|svg|png|jpg|jpeg|gif|ico|webp)$ [NC]
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    # Allow direct access to files and directories that exist
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d [OR]
    RewriteCond %{REQUEST_FILENAME} -l
    RewriteRule ^ - [L]

    # Send everything else to CodeIgniter
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>

<IfModule !mod_rewrite.c>
    ErrorDocument 404 /index.php
</IfModule>

# Disable directory browsing
Options -Indexes

# ----------------------------------------------------------------------
# Additional headers & CORS for Web Fonts
# ----------------------------------------------------------------------
<IfModule mod_headers.c>
    SetEnvIfNoCase Request_URI "\.(eot|otf|ttf|woff|woff2|svg)$" IS_FONT
    Header set Access-Control-Allow-Origin "*" env=IS_FONT

    Header always set X-Content-Type-Options nosniff
    Header always set X-Frame-Options SAMEORIGIN
    Header always set X-XSS-Protection "1; mode=block"
</IfModule>
